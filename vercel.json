{
  "installCommand": "bun install",
  "buildCommand": "bun build ./src/index.html --outdir=dist --env=inline",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
